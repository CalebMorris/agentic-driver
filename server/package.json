{
  "name": "agentic-driver-server",
  "version": "0.1.0",
  "description": "WebSocket relay server for Agentic Driver",
  "scripts": {
    "dev": "ts-node-dev src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run",
    "install:e2e": "playwright install chromium",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "ts-node-dev": "^2.0.0"
  }
}
