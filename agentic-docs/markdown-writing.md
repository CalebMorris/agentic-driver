# Markdown Writing Guidelines

- Do not use `---` horizontal rules — they clutter docs without adding structure; use headings instead.
  The only valid `---` in a file is YAML frontmatter delimiters (the opening and closing `---` of a frontmatter block).
