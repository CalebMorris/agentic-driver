{
  "name": "agentic-driver-mcp",
  "version": "0.1.0",
  "description": "MCP adapter for Agentic Driver — exposes browser actions as MCP tools via WebSocket relay",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ws": "^8.18.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.14",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
